{
  "name": "ai-pub-kit",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "deploy:pages": "wrangler pages deploy ./dist --project-name=aipubkit",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.5"
  }
}
